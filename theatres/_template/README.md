# Theatre Template

Copy this directory to create a new theatre:

```bash
cp -r theatres/_template theatres/my_theatre
```

Edit `manifest.yaml` to define your agents, pack, and analysis configuration.

Run with:
```bash
make theatre name=my_theatre
```
